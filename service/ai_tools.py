# ai_tools.py
#
# Legacy tool definitions have been removed.
# Journal indexing is now handled via the ai_tasks RabbitMQ queue (see main.py).
# Go Deeper uses RAG retrieval from Qdrant (see ai_service_processor.py).
